streamlit==1.22.0
pandas==1.5.3
datasets==2.12.0
sentence-transformers==2.2.2
numpy==1.23.5
tokenizers==0.10.3  # Older version that avoids Rust dependency
setuptools>=42  # Ensures distutils is available
pip==24.2.0  # Ensures pip is up to date for dependency resolution
